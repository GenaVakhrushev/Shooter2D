namespace Shooter.Inventory.Items.Weapons.MeleeWeapons
{
    public class Sword : MeleeWeapon
    {
        public Sword(string name, float damage, float hitDistance, float range) : base(name, damage, hitDistance, range)
        {
        }
    }
}